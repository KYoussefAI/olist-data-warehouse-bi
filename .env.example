# PostgreSQL connection used by the ETL.
# Works with the included docker-compose.yml.
DATABASE_URL=postgresql+psycopg2://olist:olist@localhost:5432/olist_dw

# Folder containing the raw Olist CSV files.
RAW_DATA_DIR=data/raw
